import { useEffect, useState } from 'react';
import { ArrowUpRight, AtSign, Check, Code2, ExternalLink, GitBranch, Mail, Menu, Moon, MoveRight, Network, Send, Sparkles, Sun, X } from 'lucide-react';
import { experience, profile, projects, skills } from './data';

function SectionHeading({ eyebrow, title, copy }) { return <div className="section-heading reveal"><p className="eyebrow">{eyebrow}</p><h2>{title}</h2>{copy && <p className="section-copy">{copy}</p>}</div>; }
function Navbar({ dark, setDark }) { const [open, setOpen] = useState(false); const links = [['About','about'],['Projects','projects'],['Experience','experience'],['Contact','contact']]; return <header className="site-header"><a className="brand" href="#top" aria-label="Back to top"><span className="brand-mark">TK</span><span>thiruwin<span className="brand-dot">.</span>dev</span></a><nav className={open ? 'nav-links is-open' : 'nav-links'} aria-label="Primary navigation">{links.map(([label,id]) => <a key={id} href={`#${id}`} onClick={() => setOpen(false)}>{label}</a>)}<a className="nav-cta" href={profile.github} target="_blank" rel="noreferrer"><GitBranch size={15} /> GitHub</a></nav><div className="header-actions"><button className="icon-button" onClick={() => setDark(!dark)} aria-label={`Switch to ${dark ? 'light' : 'dark'} mode`}>{dark ? <Sun size={17} /> : <Moon size={17} />}</button><button className="icon-button menu-button" onClick={() => setOpen(!open)} aria-label="Toggle navigation">{open ? <X size={19} /> : <Menu size={19} />}</button></div></header>; }
function Hero() { return <section className="hero" id="top"><div className="hero-copy reveal"><p className="eyebrow"><span className="status-dot" /> Available for thoughtful opportunities</p><h1>Software that feels <em>considered.</em></h1><p className="hero-lede">I’m <strong>{profile.name}</strong>, a {profile.role.toLowerCase()} focused on full-stack products, backend engineering, and applied AI.</p><div className="hero-actions"><a className="button button-primary" href="#projects">View projects <MoveRight size={17} /></a><a className="button button-ghost" href="#contact">Contact me <ArrowUpRight size={16} /></a></div><div className="social-row"><a href={profile.github} target="_blank" rel="noreferrer"><GitBranch size={17} /> GitHub</a><a href={profile.linkedin} target="_blank" rel="noreferrer"><AtSign size={17} /> LinkedIn</a></div></div><div className="hero-art reveal reveal-delay" aria-label="Abstract developer system illustration"><div className="art-grid" /><div className="terminal-card"><div className="terminal-top"><span /><span /><span /><b>system.log</b></div><div className="terminal-line"><span className="code-muted">01</span><span><i>const</i> idea = <strong>buildUsefulThings</strong>();</span></div><div className="terminal-line"><span className="code-muted">02</span><span><i>await</i> idea.<strong>ship</strong>({'{}'});</span></div><div className="terminal-line"><span className="code-muted">03</span><span className="code-success">// make it clear, then make it fast</span></div><div className="terminal-cursor" /></div><div className="orbit orbit-one" /><div className="orbit orbit-two" /><div className="art-tag tag-one"><Code2 size={14} /> clean systems</div><div className="art-tag tag-two"><Network size={14} /> connected ideas</div></div><div className="hero-foot"><span>Scroll to explore</span><span className="scroll-line" /><span>01 / 06</span></div></section>; }
function About() { return <section className="section about" id="about"><SectionHeading eyebrow="01 / About" title="Curious by default. Precise by practice." copy={profile.intro} /><div className="about-grid reveal"><div className="about-note"><span className="quote-mark">“</span><p>I like working where product intent meets engineering detail: the small decisions that make software easier to trust.</p><span className="note-sign">TK / 2026</span></div><div className="about-details"><div><span>Currently</span><strong>Learning deeper system design, retrieval engineering, and production-quality application architecture.</strong></div><div><span>Interested in</span><strong>Full-stack web development · Backend engineering · Practical AI systems</strong></div><div><span>Based in</span><strong>Pollachi, Tamil Nadu, India</strong></div></div></div></section>; }
function Skills() { return <section className="section skills-section" id="skills"><SectionHeading eyebrow="02 / Toolkit" title="A stack built for shipping." copy="A practical set of tools I use to move from a rough idea to a working, maintainable product." /><div className="skill-grid reveal">{skills.map((group,index) => <div className="skill-card" key={group.title}><span className="skill-index">0{index+1}</span><h3>{group.title}</h3><div className="skill-list">{group.items.map(item => <span key={item}><Check size={13} /> {item}</span>)}</div></div>)}</div></section>; }
function ProjectCard({ project }) { return <article className={`project-card ${project.accent} reveal`}><div className="project-visual"><span className="project-number">{project.number}</span><div className="project-visual-icon">{project.accent === 'teal' ? <Network size={40} strokeWidth={1.3} /> : project.accent === 'coral' ? <Sparkles size={40} strokeWidth={1.3} /> : <Code2 size={40} strokeWidth={1.3} />}</div><span className="project-label">{project.label}</span></div><div className="project-content"><div><h3>{project.name}</h3><p>{project.description}</p></div><div className="tag-list">{project.tags.map(tag => <span key={tag}>{tag}</span>)}</div><div className="project-links"><a href={project.github} target="_blank" rel="noreferrer"><GitBranch size={15} /> Source <ExternalLink size={13} /></a>{project.demo && <a href={project.demo} target="_blank" rel="noreferrer">Live demo <ArrowUpRight size={14} /></a>}</div></div></article>; }
function Projects() { return <section className="section projects-section" id="projects"><SectionHeading eyebrow="03 / Selected work" title="Projects with a point of view." copy="A few systems I’ve built to explore how software can make complex work feel simpler." /><div className="project-grid">{projects.map(project => <ProjectCard project={project} key={project.name} />)}</div><a className="text-link reveal" href={profile.github} target="_blank" rel="noreferrer">Explore all repositories <MoveRight size={16} /></a></section>; }
function Experience() { return <section className="section experience-section" id="experience"><SectionHeading eyebrow="04 / Experience" title="Still early. Already building." copy="The through-line so far: learn quickly, own the details, and leave the system better than I found it." /><div className="timeline">{experience.map((item,index) => <article className="timeline-item reveal" key={item.role}><div className="timeline-marker">0{index+1}</div><div className="timeline-content"><div className="timeline-meta"><span>{item.period}</span><span>{item.company}</span></div><h3>{item.role}</h3><p>{item.detail}</p><div className="tag-list">{item.stack.map(tag => <span key={tag}>{tag}</span>)}</div></div></article>)}</div></section>; }
function GithubSection() { return <section className="section github-section"><div className="github-panel reveal"><div className="github-copy"><p className="eyebrow">05 / Open source</p><h2>Built in public,<br /><em>learning in public.</em></h2><p>My GitHub is where experiments become systems. Browse the repositories, trace the decisions, and see what I’m building next.</p><a className="button button-light" href={profile.github} target="_blank" rel="noreferrer">Visit GitHub profile <ArrowUpRight size={16} /></a></div><div className="activity-card"><div className="activity-head"><span><GitBranch size={17} /> contribution activity</span><span className="activity-year">2026</span></div><div className="activity-grid">{Array.from({ length:91 },(_,index) => <span key={index} className={`activity-cell level-${(index*7+3)%5}`} />)}</div><div className="activity-foot"><span>Less</span><i className="activity-cell level-0" /><i className="activity-cell level-2" /><i className="activity-cell level-4" /><span>More</span></div></div></div></section>; }
function Contact() { const [sent,setSent] = useState(false); return <section className="section contact-section" id="contact"><SectionHeading eyebrow="06 / Contact" title="Have a good problem?" copy="I’m always interested in thoughtful products, curious teams, and conversations about how software gets made." /><div className="contact-grid reveal"><div className="contact-details"><a href={`mailto:${profile.email}`}><Mail size={18} /><span><small>Email</small>{profile.email}</span><ArrowUpRight size={15} /></a><a href={profile.linkedin} target="_blank" rel="noreferrer"><AtSign size={18} /><span><small>LinkedIn</small>Connect with me</span><ArrowUpRight size={15} /></a><a href={profile.github} target="_blank" rel="noreferrer"><GitBranch size={18} /><span><small>GitHub</small>@thiuwinKarthik</span><ArrowUpRight size={15} /></a></div><form className="contact-form" onSubmit={event => { event.preventDefault(); setSent(true); }}><label>Your name<input required name="name" placeholder="Jane Doe" /></label><label>Your email<input required type="email" name="email" placeholder="jane@company.com" /></label><label>What’s on your mind?<textarea required name="message" rows="4" placeholder="Tell me a little about the opportunity..." /></label><button className="button button-primary" type="submit">{sent ? <>Message prepared <Check size={16} /></> : <>Send a note <Send size={15} /></>}</button>{sent && <p className="form-note">Thanks. This demo form is ready for an email service or backend integration.</p>}</form></div></section>; }
function Footer() { return <footer className="site-footer"><div className="footer-brand"><span className="brand-mark">TK</span><span>thiruwin<span className="brand-dot">.</span>dev</span></div><p>Designed and built with React · 2026</p><div className="footer-links"><a href={profile.github} target="_blank" rel="noreferrer">GitHub</a><a href={profile.linkedin} target="_blank" rel="noreferrer">LinkedIn</a><a href={`mailto:${profile.email}`}>Email</a></div></footer>; }
export default function App() { const [dark,setDark] = useState(true); useEffect(() => { document.documentElement.dataset.theme = dark ? 'dark' : 'light'; },[dark]); useEffect(() => { const observer = new IntersectionObserver(entries => entries.forEach(entry => { if(entry.isIntersecting) entry.target.classList.add('is-visible'); }),{ threshold:.12 }); document.querySelectorAll('.reveal').forEach(el => observer.observe(el)); return () => observer.disconnect(); },[]); return <><Navbar dark={dark} setDark={setDark} /><main><Hero /><About /><Skills /><Projects /><Experience /><GithubSection /><Contact /></main><Footer /></>; }
